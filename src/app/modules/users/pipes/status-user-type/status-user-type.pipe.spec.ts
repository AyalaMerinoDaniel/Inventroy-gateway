import { StatusUserTypePipe } from './status-user-type.pipe';

describe('StatusUserTypePipe', () => {
  it('create an instance', () => {
    const pipe = new StatusUserTypePipe();
    expect(pipe).toBeTruthy();
  });
});
