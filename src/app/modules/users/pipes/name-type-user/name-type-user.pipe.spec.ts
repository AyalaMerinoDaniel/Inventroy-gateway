import { NameTypeUserPipe } from "./name-type-user.pipe";

describe('NameTypeUserPipe', () => {
  it('create an instance', () => {
    const pipe = new NameTypeUserPipe();
    expect(pipe).toBeTruthy();
  });
});
